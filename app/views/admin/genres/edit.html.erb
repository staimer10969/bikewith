<h1>ジャンル編集</h1>

 <%= form_with model: @genre, url:admin_genre_path, local:true do |f| %>
  <div>
    <%= f.label :genre_name,"ジャンル名" %>
    <%= f.text_field :genre_name %>
  </div>

  <div class="btn-class ml-5">
    <%= f.submit "変更を保存", class: "btn btn-success" %>
  </div>
<% end %>